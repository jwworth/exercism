#!/usr/bin/ruby

puts 'What exercise would you like to submit?'
exercise = gets.chomp

puts 'In what language?'
language = gets.chomp

extension = language == 'ruby' ? 'rb' : ''
`exercism submit #{language}/#{exercise}/#{exercise}.#{extension}`
